import { StopContextMenuDirective } from './stop-context-menu.directive';

describe('StopContextMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new StopContextMenuDirective();
    expect(directive).toBeTruthy();
  });
});
